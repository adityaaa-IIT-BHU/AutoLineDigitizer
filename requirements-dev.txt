# Run-from-source environment for the full AutoLineDigitizer pipeline
# (PDF gallery + NCMRD integration + Claude curation + Starrydata uploads).
# Python 3.11. Versions are the tested combination.
#
# INSTALL ORDER MATTERS — torch first, then mmcv-full (compiles against it):
#
#   conda create -n alddev python=3.11 -y --override-channels -c conda-forge
#   conda activate alddev
#   pip install torch==2.12.0 torchvision==0.27.0
#   pip install --no-build-isolation mmcv-full==1.7.2   # slow compile; the
#       # --no-build-isolation is REQUIRED: setuptools>=81 removed
#       # pkg_resources, which mmcv's isolated build env expects
#   pip install -r requirements-dev.txt
#
# (conda-forge channel avoids Anaconda's defaults-channel ToS prompt.)
#
torch==2.12.0
torchvision==0.27.0
mmcv-full==1.7.2
mmdet==2.28.2
easyocr==1.7.2
flet==0.24.1
opencv-python==4.13.0.92
PyMuPDF==1.27.2.3
numpy==2.4.4
scipy==1.17.1
scikit-image==0.26.0
shapely==2.1.2
pillow==12.2.0
jsonschema==4.26.0
anthropic==0.105.2
requests==2.28.2
# core digitization path (submodules/lineformer/line_utils.py)
bresenham==0.2.1
# MinerU PP-DocLayoutV2 figure detector
transformers==4.57.6
huggingface_hub==0.36.2
