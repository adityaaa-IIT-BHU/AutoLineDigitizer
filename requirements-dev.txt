# Run-from-source environment for the full AutoLineDigitizer pipeline
# (PDF gallery + KMDS integration + Claude curation + Starrydata uploads).
# Python 3.11. Versions are the tested combination — mmdet 2.x needs mmcv-full 1.x,
# which builds against your local torch; install torch FIRST, then mmcv-full.
#
#   conda create -n alddev python=3.11 -y && conda activate alddev
#   pip install torch==2.12.0
#   pip install mmcv-full==1.7.2        # compiles against torch; takes a while
#   pip install -r requirements-dev.txt
#
torch==2.12.0
mmcv-full==1.7.2
mmdet==2.28.2
easyocr==1.7.2
flet==0.24.1
opencv-python==4.13.0.92
PyMuPDF==1.27.2.3
numpy==2.4.4
scipy==1.17.1
scikit-image==0.26.0
shapely==2.1.2
pillow==12.2.0
jsonschema==4.26.0
anthropic==0.105.2
requests==2.28.2
